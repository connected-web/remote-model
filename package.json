{
  "name": "remote-model",
  "version": "1.0.3",
  "description": "A fetcher for an externally hosted config file, that is released on a different cadence to your service",
  "main": "src/index.js",
  "scripts": {
    "test": "standard && mocha",
    "lint": "standard --fix"
  },
  "author": "John Beech",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/connected-web/remote-model.git"
  },
  "standard": {
    "env": [
      "mocha"
    ]
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "mocha": "^9.1.2",
    "sinon": "^10.0.0",
    "standard": "^16.0.4"
  }
}
